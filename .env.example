# Server-only. Create `.env.local` and paste your key there for local AI imports.
OPENAI_API_KEY=your_openai_api_key

# Optional. Study Drift defaults to this balanced text-and-vision model.
OPENAI_MODEL=gpt-5.6-terra
